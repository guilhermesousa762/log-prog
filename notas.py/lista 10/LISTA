#CONSTRUA UM PROGRAMA ONDE O USUARIO DIGITA 
#DEZ MUMEROS E O PROGRAMA
#SOMARA E CALCULARA A MEDIA DOS NUMEROS DIGIT

#lista = []
#for i in range (0,3):
   # numero = float(input('digite um numero: '))
   # lista.append(numero)

#media = sum(lista)/len(lista)
#print(f'A media é: {media}')



#ou
# soma = 0
#for item in lista :
#   soma = soma + item
#Construa um programa onde o usuário digitará dez números. O programa
#deverá calcular quais deles são maiores que dez.

numeros = []

for i in range(0, 3):
    numero = float(input('digite um numero: '))
    numeros.append(numero)
contado = 0
for numero in numeros:
    if numero > 10:
       contador += 1
print(contador)
