cargo = input('digite o cargo: ')
hora = int(input('digite o horario atual (em horas): '))
chave_emergencia = int(input(' possui chave d emergencia? 1 para sim, 0 para nao: '))

if(chave_emergencia == 1 or cargo == 'supervisor' or
    (cargo == 'operador' and 8 <= hora <= 17)):
    print ('acesso permitido')
else:
    print ('acesso negado') 